<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- WARNING: Eclipse auto-generated file.
              Any modifications will be overwritten.
              To include a user specific buildfile here, simply create one in the same
              directory with the processing instruction <?eclipse.ant.import?>
              as the first entry and export the buildfile again. -->
<project xmlns:ivy="antlib:org.apache.ivy.ant"  basedir="." default="build-min" name="YamlSpringLoader">

	<property environment="env"/>
	<property file="ivy.properties"/>

	<tstamp>
		<format property="timestamp" pattern="yyDDDkkmmss" />
	</tstamp>

	<property name="major" value="2"/>
	<property name="minor" value="7"/>
	<property name="version" value="${major}.${minor}"/>

	<taskdef resource="org/apache/ivy/ant/antlib.xml"
        uri="antlib:org.apache.ivy.ant" classpath="${ivyjar}"/>

	<property name="debuglevel" value="source,lines,vars"/>
	<property name="target" value="1.6"/>
	<property name="source" value="1.6"  />

	<property name="dependency.jar.dir" value="lib" />
	<property name="ant.destdir" value="classes" />
	<property name="ant.target.dir" value="target" />	
	
	<path id="YamlSpringLoader.classpath">
		<pathelement location="bin"/>
		<fileset dir="lib">
			<include name="**/*.jar"/>
		</fileset>
	</path>

	<!-- ================================= 
  	          target: resolve              
 	     ================================= -->
	
	<target name="init">
		<mkdir dir="${ant.target.dir}" />
		<mkdir dir="${ant.destdir}" />
	</target>
	
	<target name="resolve-runtime" depends="init" description="--> retrieve dependencies with ivy">
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${dependency.jar.dir}" />
		</delete>
		
		<ivy:retrieve sync="true" conf="runtime"/>
	</target>

	<target name="clean">
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${ant.destdir}" />
			<fileset dir="${ant.target.dir}" />
			<fileset dir="${dependency.jar.dir}" />
		</delete>
	</target>

	<target name="build-min" depends="resolve-runtime">
		<echo message="${ant.project.name}: ${ant.file}"/>
		<mkdir dir="bin"/>

		<javac debug="true" debuglevel="${debuglevel}" destdir="bin" source="${source}" target="${target}">
			<src path="src/main/java"/>
			<classpath refid="YamlSpringLoader.classpath"/>
		</javac>
		<javac debug="true" debuglevel="${debuglevel}" destdir="bin" source="${source}" target="${target}">
			<src path="src/test/java"/>
			<classpath refid="YamlSpringLoader.classpath"/>
		</javac>
		<javac debug="true" debuglevel="${debuglevel}" destdir="bin" source="${source}" target="${target}">
			<src path="src/main/resources"/>
			<classpath refid="YamlSpringLoader.classpath"/>
		</javac>

		<mkdir dir="target"/>

		<jar destfile="target/YamlSpringLoader-${version}.jar" >
			<fileset dir="bin" />
		</jar>

	</target>

	<target name="publish" depends="build-min" >
		<ivy:publish resolver="workspace" pubrevision="${version}" status="release" update="true" overwrite="true" conf="runtime">
			<artifacts pattern="target/[artifact]-[revision].[ext]" />
		</ivy:publish>
	</target>

</project>
